def start_rest_interface(config, process_name):
    """
    Start the REST interface for communication with the specified process.

    Args:
        config (dict): Configuration dictionary for the REST interface.
        process_name (str): Name of the process to manage.
    """
    raise NotImplementedError("gRPC interface is not implemented yet.")
