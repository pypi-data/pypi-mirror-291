[tool.poetry]
name = "redstone-lib"
version = "0.1.0"
description = "a package that does something very cool"
authors = ["Your Name <teamsdi1234@gmail.com>"]
readme = "README.md"

packages = [
    { include = "redstone", from = "src" }
]

[tool.poetry.dependencies]
python = "^3.11"


[build-system]
requires = ["poetry-core"]
build-backend = "poetry.core.masonry.api"
