[tool.poetry]
name = "bleongpkg"
version = "0.1.0"
description = ""
authors = ["benleong0 <benleong@myurbanjungle.com>"]
readme = "README.md"

[tool.poetry.dependencies]
python = "^3.12"
arrow = { version = "^1.3.0", optional = true }

[tool.poetry.group.dev.dependencies]
ruff = "^0.6.1"

[tool.poetry.extras]
time = ["arrow"]

[tool.poetry.scripts]
main = 'bleongpkg.main:main'

[build-system]
requires = ["poetry-core"]
build-backend = "poetry.core.masonry.api"
