[project]
name = "bfastor"
version = "0.2.0"
description = "Fast cryo-EM B-factor refinement"
readme = "README.md"
requires-python = ">=3.9"
authors = [
  {name = "Joseph Beton", email = "joseph.beton@cssb-hamburg.de" }
]
maintainers = [
  {name = "Joseph Beton", email = "joseph.beton@cssb-hamburg.de" }
]
classifiers = [
  "Development Status :: 3 - Alpha",
  "Intended Audience :: Science/Research",
  "Programming Language :: Python :: 3",
  "Programming Language :: Python :: 3.9",
  "Programming Language :: Python :: 3.10",
  "Programming Language :: Python :: 3.11",
  "Programming Language :: Python :: 3 :: Only",
]
dependencies = [
    "chex>=0.1.85",
    "jax>=0.4.30",
    "optax>=0.1",
    "flax>=0.8",
    "numpy>=1.17",
    "pandas>=2.2",
    "gemmi>=0.5.8",
    "mrcfile>=1.5",
    "ratelimit>=2.2",
    "voxcov>=0.2.8",
    "matplotlib>=3.6",
    "tqdm>=4.0",
]
[project.optional-dependencies]
dev = ["check-manifest", "pytest", "black"]
test = ["coverage", "pytest",]
cuda12 = ["jax[cuda12]",]
[project.urls]
"Homepage" = "https://gitlab.com/topf-lab/jaxref"
"Bug Reports" = "https://gitlab.com/topf-lab/jaxref/issues"
"Source" = "https://gitlab.com/topf-lab/jaxref/"

[build-system]
requires = ["setuptools>=61.0.0", "wheel"]
build-backend = "setuptools.build_meta"

[project.scripts]
bfastor-refine = "bfastor.scripts.refine_bfactors:main"

# This is configuration specific to the `setuptools` build backend.
# If you are using a different build backend, you will need to change this.
[tool.setuptools]

[tool.pytest.ini_options]
pythonpath = [
  "."
]
markers = [
    "slow: marks tests as slow (deselect with '-m \"not slow\"')",
]
[tool.coverage.run]
omit = ["bfastor/scripts/*",]
