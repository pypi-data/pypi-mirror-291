[build-system]
requires = [
    'setuptools>=42',
    'wheel'
]
build-backend = 'setuptools.build_meta'

[project]
name = 'biwoco'
description = 'A sample python package'
version = '0.0.2'
readme = 'README.md'
requires-python = '>=3.10'
dependencies = [
                'requests'
                ]