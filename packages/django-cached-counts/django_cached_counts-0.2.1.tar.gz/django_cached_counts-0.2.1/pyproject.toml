[project]
name = "django_cached_counts"
version = "0.2.1"
description = "Caching count() rqueries"
readme = "README.md"
authors = [
  {name = "pawnhearts", email = "robotnaoborot@gmail.com"}
]
maintainers = [
  {name = "pawnhearts", email = "robotnaoborot@gmail.com"}
]

license = {text = "MIT license"}
dependencies = [
  
]
[tool.poetry]
name = "django_cached_counts"
version = "0.2.1"
description = "Caching count() in django models"
authors = ["ph <robotnaoborot@gmail.com>"]
license = "MIT"
readme = "README.md"

[tool.poetry.dependencies]
python = "^3.8"
django = "^3.2"

[tool.poetry.dev-dependencies]

[build-system]
requires = ["poetry>=0.12"]
build-backend = "poetry.masonry.api"



[tool.setuptools]
package-dir = {"" = "."}

[project.urls]
bugs = "https://github.com/pawnhearts/django_cached_counts/issues"
changelog = "https://github.com/pawnhearts/django_cached_counts/blob/master/changelog.md"
homepage = "https://github.com/pawnhearts/django_cached_counts"
