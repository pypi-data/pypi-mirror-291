name: On tag pushed

concurrency:
  group: tag-${{ github.ref }}
  cancel-in-progress: true

on:
  push:
    tags:
      - '**'

permissions:
  contents: write

jobs:
  release:
    uses: ./.github/workflows/call_release.yml
    secrets: inherit
    with:
      tag: ${{ github.ref_name }}