[build-system]
requires = ["setuptools"]
build-backend = "setuptools.build_meta"


[project]
name = "discord-webhook-types"
version = "0.0.1"
authors = [
{ name = "Ethan Rietz", email="ewrietz@gmail.com" }
]
readme = "README.md"
dependencies = [
]
